{
  "type": "module",
  "devDependencies": {
    "@types/three": "^0.154.0",
    "site": "^2.3.1",
    "vite": "^4.4.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx vite build"
  }
}
